import Auth from '../components/Auth';
import Layout from '../components/Layout';

const Home = () => {
  return (
    <Layout title="login">
      <Auth />
    </Layout>
  );
};

export default Home;
